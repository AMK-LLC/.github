name: Security Scan
on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]
jobs:
  security:
    uses: AMK-LLC/.github/.github/workflows/reusable-security.yml@main
